# Application mobile Boutique Manager

Les instructions complètes se trouvent dans le [README principal](../README.md).

```bash
npm install
cp .env.example .env
npx expo start
```

Sur un téléphone physique, `EXPO_PUBLIC_SERVER_URL` doit contenir l’IP réseau du PC (pas `localhost`) ou l’URL HTTPS du backend Vercel.
