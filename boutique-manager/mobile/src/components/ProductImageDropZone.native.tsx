import type { PickedImage } from '../types/product';

export function ProductImageDropZone(_props: {
  disabled: boolean;
  remaining: number;
  onImages: (images: PickedImage[]) => void;
}) {
  return null;
}
