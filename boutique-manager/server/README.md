# Backend Boutique Manager

Les instructions complètes (local, MongoDB Atlas, Vercel et Blob) se trouvent dans le [README principal](../README.md).

```bash
npm install
cp .env.example .env
npm run dev
```

L’API locale écoute par défaut sur `http://localhost:4000` et son health check est `GET /api/health`.
